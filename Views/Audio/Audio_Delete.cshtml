<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Rooms</h4>
    <hr />
    <dl class="dl-horizontal">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Recent Tickets</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>AUDIO_ID</th>
                                <th> AUDIO_URL </th>
                                <th>AUDIO_DESCRIPTION</th>
                                <th>LESSON_ID</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td> 123</td>
                                <td>123</td>
                                <td>123</td>
                                <td>123</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div style="padding-top:10px" class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-gradient-primary me-2" />
                        @Html.ActionLink("Cancel", "Audio_List", "Audio", new { @class = "btn btn-light" })
                    </div>
                }
            </div>
        </div>
    </dl>
</div>